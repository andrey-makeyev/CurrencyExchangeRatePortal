package com.portal.exchangerate.enums;

public enum OperationType { //TODO fetch from database and push via controllers
    GET_CURRENCY_LIST,
    GET_CURRENT_FX_RATES,
    GET_FX_RATES,
    GET_FX_RATES_FOR_CURRENCY
}